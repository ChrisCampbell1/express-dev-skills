<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <title>Edit Skill</title>
</head>
<body>
  <header>
    <h1>Edit Skill</h1>
  </header>
  <form action="/skills/<%= skill._id %>/?_method=PUT" method="POST" autocomplete="off">
    <input type="text" name="name" value="<%= skill.name%>">
    <select name="type" id="" autocomplete="<%= skill.type %>">
      <option value="Skill Type">Select Skill Type</option>
      <option value="Front End" <%= skill.type === "Front End" ? "selected" : ""%>>Front End</option>
      <option value="Back End" <%= skill.type === "Back End" ? "selected" : ""%>>Back End</option>
      <option value="Full Stack" <%= skill.type === "Fullstack" ? "selected" : ""%>>Full Stack</option>
    </select>
    <select name="proficiency" id="">
      <option value="Proficiency">Select Proficiency Level</option>
      <option value="Low" <%= skill.proficiency === "Low" ? "selected" : ""%>>Low</option>
      <option value="Medium" <%= skill.proficiency === "Medium" ? "selected" : ""%>>Medium</option>
      <option value="High" <%= skill.proficiency === "High" ? "selected" : ""%>>High</option>
    </select>
    <button type="submit">Save Skill</button>
  </form>
</body>
</html>
